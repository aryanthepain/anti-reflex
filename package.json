{
  "name": "anti-reflex",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Anti-Reflex — a reaction game where fast reflexes make you lose.",
  "scripts": {
    "start": "npx --yes http-server -c-1 -o",
    "serve": "npx --yes http-server -c-1",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  }
}
